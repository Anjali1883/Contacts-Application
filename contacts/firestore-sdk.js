 // Import the functions you need from the SDKs you need
  // paste your project configuration. Found in the PROJECT SETTINGS in firebase

  // Initialize Firebase
  const app = initializeApp(firebaseConfig);